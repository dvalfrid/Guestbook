server:
  type: simple
  rootPath: /api/
  applicationContextPath: /
  adminContextPath: /admin
  connector:
    type: http
    port: 3000

database:
  driverClass: org.postgresql.Driver
  url: jdbc:postgresql://localhost:5432/gastolibro
  validationQuery: "SELECT 1"
